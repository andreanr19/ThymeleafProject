spring:
    jpa:
        database: POSTGRESQL
        show-sql: false
        hibernate.ddl-auto: update
    datasource:
        platform: postgres
        url: jdbc:postgresql://localhost:5432/pg_estud
        username: postgres
        password: postgres  
        driverClassName: org.postgresql.Driver

